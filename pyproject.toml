[project]
name = "containerctl"
version = "0.1.0"
requires-python = ">=3.11"

[tool.uv]
dev-dependencies = ["ruff"]

[tool.ruff]
line-length = 120
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
