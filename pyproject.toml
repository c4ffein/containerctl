[project]
name = "containerctl"
version = "0.1.0"
requires-python = ">=3.10"

[tool.uv]
dev-dependencies = ["ruff"]

[tool.ruff]
line-length = 120
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
